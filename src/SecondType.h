#ifndef SECOND_TYPE
#define SECOND_TYPE
#include <string>
#include <vector>
#include "Questions.h"
#include "parseString.h"
void SecondType(std::ifstream &questions, std::vector<Questions>& text);

#endif
