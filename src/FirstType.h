#ifndef FIRST_TYPE
#define FIST_TYPE
#include <string>
#include <vector>
#include "Questions.h"
#include "parseString.h"
void FirstType(std::ifstream &questions, std::vector<Questions>& text);

#endif
